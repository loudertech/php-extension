// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ejemplo", "for ejemplo support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ejemplo", "enable ejemplo support", "no");

if (PHP_EJEMPLO != "no") {
	EXTENSION("ejemplo", "ejemplo.c");
}

